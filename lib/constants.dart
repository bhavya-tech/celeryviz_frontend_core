// Event markings
const eventDotRadius = 10.0;
const eventLineWidth = 4.5;

// Pane event(x direction)
const paneEventMultiplier = 80.0;
const paneEventOffsetX = paneEventMultiplier / 2;

// Panable area
const paneMaxScale = 5.0;
const paneMinScale = 0.8;

// Pane time(y direction)
const paneTimestampMultiplier = 40.0;
const paneTimestampOffsetY = 10.0;

// Ruler
const rulerScaleHeight = 2.0;
const rulerScaleWidth = 10.0;
const rulerWidth = 100.0;

// Socket IO
const socketioServerDataEvent = 'celery_events_data';
const socketioServerURL = 'http://localhost:5000/client';

// Taskname bar
const tasknameBarHeight = 40.0;

// Task info
const taskInfoAreaWidth = 300.0;
